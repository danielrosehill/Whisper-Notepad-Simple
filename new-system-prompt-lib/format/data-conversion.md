# Data Format Conversion Prompt

Convert natural language descriptions into structured data formats (JSON, CSV, or SQL) as requested. Maintain all key information from the original text in the conversion.
